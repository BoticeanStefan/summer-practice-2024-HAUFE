// JavaScript source code Navigation Bar
import React from 'react';

const Navbar = () => {
  return (

<nav style={styles.navbar}>
    </h1>Companies for friends</n1>
</nav>
);
};

const styles = {
    navbar : {
        padding: '1rem',
        backgroundColor: '#282c34',
        color: 'white',
        textAlign: 'center',
    }
};

export default Navbar;